# RAG Backend

FastAPI backend for local RAG document ingestion.

## Setup

```bash
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install -e ".[dev]"
```

## Verify

```bash
python -m pytest -v
```
